import type { Meta } from '@storybook/react';
import { MainFooter } from './main-footer';

const Story: Meta<typeof MainFooter> = {
  component: MainFooter,
  title: 'MainFooter',
};
export default Story;

export const Primary = {
  args: {},
};
